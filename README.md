# Help Desk — Ticket Practice Log

A running log of help desk tickets I've worked through in a service desk
simulator(https://servicedesk-simulator.com), documented the way a real Level 1 technician documents a ticket:
symptom, diagnosis, root cause, fix, and the lesson learned.

> **Scope note:** This is self-directed practice in a training simulator, done in
> my own time to build procedural fluency with ticket workflows and knowledge-base
> runbooks. It is not paid or on-the-job experience. The user names and data shown
> are fictional characters generated by the simulator.

---

## Why I keep this log

Working a ticket well is not just fixing the problem — it's isolating the cause
before acting, matching the fix to the symptom, and writing notes clear enough for
the next technician. This log is where I practise that discipline. Each entry
records not only *what* fixed the issue but *why* it worked, and where my first
instinct was wrong.

## How I triage a ticket

Every ticket follows the same repeatable loop:

![Ticket triage flow](screenshots/servicedesk-triage-flow.svg)

A recurring lesson across these tickets: **"it works now" is not the same as
"I found why."** A reboot can mask a cause rather than fix it.

---

## Ticket log

| # | Category | Symptom | Root cause | Fix | Key lesson |
|---|---|---|---|---|---|
| 01 | Hardware | Desktop completely dead, no power, no POST | Hardware failure (confirmed dead on a known-good outlet) | Replaced the unit and re-imaged via the imaging server; automated domain join | Confirm dead on a known-good outlet before declaring hardware failure — replace, don't repair |
| 02 | Network / Access | Remote user can't reach the Marketing shared drive ("network path not found") | User was off the corporate network with no VPN tunnel | Connected the VPN client, verified the share path, remapped the drive | "Network path not found" for a remote user with working internet = missing VPN, not permissions |
| 03 | Identity / Access | Password stopped working after a 3-week absence | Password had expired while the user was away | Verified identity first, then reset with a temp password + force-change at next logon | Verify identity **before** any account action; expired-after-absence is routine, not a compromise |
| 04 | Software / Network (VoIP) | Audio cuts out mid-call on a softphone | Unconfirmed — driver update + reboot cleared it | Updated the headset driver and rebooted | On VoIP, clean mid-call drop-outs point to network/jitter, not drivers; changing two things at once left the true cause unproven |
| 05 | Email / Identity | Email won't set up on new phone AND fails on workstation | Password issue (not "account disabled" — a distinct state) | Ran the runbook isolation step, verified identity, reset password with force-change | Isolate scope first (does it fail on other devices too?); match note wording to the actual account state |
| 06 | Network / Access | Remote user's VPN won't reconnect | Unconfirmed — flushdns + reboot restored it | Ran `ipconfig /flushdns` and rebooted | `flushdns` clears the DNS resolver cache (not IPs, not a "VPN cache"); change one variable at a time so the cause is provable |
| 07 | Infrastructure / Print Services | All printers, every floor, offline at once (worked the previous evening) | Print services not running on the print server (server-down vs spooler-hung not separately confirmed) | Isolated to the print server, rebooted it, confirmed active, user verified printing restored | Simultaneous multi-site failure = shared dependency, not the endpoints; check whole-server-vs-spooler before rebooting |
| 08 | Identity / Access | New hire access setup — create account + provision group membership | N/A — service request (no fault) | Verified correct target account, created user in Engineering OU with force-password-change, added the 3 requested groups, confirmed account enabled + memberships present | Verify by unique account when names collide; provisioning is a request (fulfilment), not an incident (root cause) |
| 09 | Identity / Access | Department transfer — grant new-role access, revoke previous access | N/A — service request (no fault) | Removed Engineering group, added IT Infrastructure group, confirmed both changes applied | Revoking old access matters as much as granting new (least privilege / access creep); confirm both halves |
| 10 | Hardware / Power | Laptop won't boot - black screen, slow-blinking power light, unresponsive | Hung power state (specific origin unconfirmed) | Removed peripherals to isolate, then forced power cycle (30-sec power-button hold) to drain residual power and hard-restart | Blinking light + black screen = power-state hang, not a dead unit; isolate externals before the forced fix |
| 11 | Security / Identity | Alert of sign-in from an unrecognised location; suspected account compromise | N/A — suspected breach (escalated, not closed) | Verified identity, reset password from clean path, reset MFA, ran malware scan, escalated to security | Password change alone isn't containment - reset MFA too; a suspected breach on a sensitive account is escalated, not closed at L1 |
| 12 | Software / Cloud | Cloud Drive errors, files duplicating continuously (after user installed unauthorised software) | Sync conflicts cleared by resync; unauthorised software a likely cause, not removed | Remote in, forced Cloud Drive resync, confirmed files synced correctly | Clearing the symptom ≠ removing the cause; unauthorised internet software is a security item warranting removal + scan |
| 13 | Network / Infrastructure | Floor 2 network drops ~30s every few minutes, wired + wireless, other floors fine | Floor 2 switch reported down (hard-fail vs intermittent flap not investigated) | Isolated to the floor switch from scope, confirmed status down in devices section, rebooted, user verified, posted known-issue notice | Wired + wireless down on one floor = shared switch; an intermittent fault may not be cleared by a reboot — monitor for recurrence |
| 14 | Identity / MFA | User can't log in.OTP not arriving at registered phone; credentials succeed | MFA method failing to deliver codes (carrier vs drift vs corrupted enrolment not isolated) | Notified manager, verified identity, reset MFA enrolment (new QR), user re-enrolled and confirmed access | Isolate which factor is failing — credentials OK + no OTP = second-factor fault; never deliver credentials over chat |
| 15 | Network / Access | Remote user can't reach internal HR portal (`.corp.local`); public internet fine | VPN tunnel not active — internal DNS names can't resolve off the corporate network | Remoted in, confirmed tunnel inactive, restored VPN, verified portal reachable, user confirmed | Internal-only failure + working internet = check the VPN first; `.local` names have no public DNS record, so off-VPN the failure is at name resolution |
| 16 | Software / Cloud | Folder deleted + local Trash emptied — months of client proposals at risk | Local Trash cleared, but cloud service retains its own deleted-item store within a retention window | Synced Cloud Drive, restored folder from Recently Deleted Files (nested in Version History), opened recovered file to confirm content intact | Emptying local Trash ≠ permanently gone if cloud-synced; check retention timing, and verify content integrity, not just reappearance |
| 17 | Network / Remote Connectivity | Remote worker's WiFi won't connect; no one else affected | Home network WiFi profile mismatched with router (specific origin — changed password, stale cache, mismatched key — not isolated) | Forgot the saved home WiFi profile, reconnected with the current key | For remote users, "only me affected" points at their home network, not company infrastructure; forget-and-reconnect resolves several causes identically, so the specific cause stays unconfirmed |
| 18 | Security / Device Encryption | Laptop stuck at BitLocker recovery screen, no key, fully locked out | BitLocker triggered by an untrusted boot-state change (exact trigger not isolated) | Verified identity, matched recovery key to the specific key ID on screen, applied key (sim allowed direct entry; production method is reading the key aloud pre-boot) | Match recovery key to device key ID, not just the user's account; BitLocker recovery is pre-boot/pre-network, so remoting in typically isn't possible — key is read aloud, not typed in remotely |
| 19 | Hardware / Software | Second monitor "No Signal" with flickering; monitor confirmed working on another PC | Graphics driver unreliably recognising the second output | Checked for updates via System Update, installed graphics driver update, restarted, monitor detected and stable | Fault following the PC across multiple cables/ports/monitors points to the driver, not hardware; flickering (vs flat failure) signals an unstable driver handshake |
| 20 | Network / Infrastructure | Office-wide extreme slowdown (2 Mbps vs 500 Mbps expected), WAN apps affected | Core router offline; router-vs-switch distinguished by WAN-bound vs LAN-only impact (reported speed doesn't fully reconcile with a fully-down status) | Isolated to core router from office-wide/multi-service scope, confirmed offline status in server room, rebooted, confirmed restored | Distinguish router vs core switch via LAN-only vs WAN-bound impact; reconcile reported symptoms against confirmed status rather than accepting a mismatch silently |
| 21 | Hardware / Network — Print Services | Network printer shows offline despite being on with paper; worked a couple weeks ago | Printer's IP address had changed; print queue still pointed at the old address | Ruled out driver (updates current), removed and re-added printer using its current IP from documentation, test page confirmed | "Offline" on a network device often means a stale address, not a driver fault; recommend static IP/DHCP reservation for shared devices to prevent recurrence |
| 22 | Hardware — Peripherals | Both desk monitors black when docked; laptop's own screen fine, daisy-chained setup | First cable in the daisy chain (dock to Monitor 1) — inferred from topology, not visually confirmed | Basic reseating/restart tried first, photo confirmed cable type only, replacement cable shipped via Ship Manager (rush priority), monitors restored | Daisy-chained displays share one point of failure at the first link; a photo can confirm part type without confirming a fault — don't overstate what evidence actually shows |
---

## The documentation loop

Each ticket write-up in [`tickets/`](tickets/) uses the same five sections:

1. **Diagnosis** — how I isolated the cause.
2. **Root cause** — what was actually wrong (honestly marked *unconfirmed* where
   the cause wasn't proven).
3. **Action taken** — what I did.
4. **Verification** — how I confirmed it worked.
5. **Status** — resolved / resolved-cause-unproven.

Plus a **Lesson learned** for each, because the reasoning is the point.

Passwords are never recorded in notes. Identity is always verified before any
account action.

---

## Detailed write-ups

Full write-ups for individual tickets are in the [`tickets/`](tickets/) folder,
starting with [Ticket 01 — Dead Desktop](tickets/).

---

*Documented by Mertcan (Matt) Alkaya — Brisbane, QLD. Self-directed service desk
practice.*
